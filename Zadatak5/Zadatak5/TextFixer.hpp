#ifndef  TEXTFIXER_HPP
#define TEXTFIXER_HPP
#include <iostream>
#include <string>

void fixText(std::string& text);
void textFunction(std::string& text);
void leftSide(std::string& text, int n, int from);
void rightSide(std::string& text, int n, int from);

#endif // ! VEKTOR_HPP
